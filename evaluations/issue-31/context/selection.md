# Selection and context diagnosis before runtime edits

B2: `4b895265d8262fafd7a95221a344a7de8c93841e`; protocol `ab3af52b9f8efc7c3a78b64f118e3f34fb22d07d`.
Both fresh responses completed before this selection. The root inspected their
evidence against B2 source/decisions/tests and actual history.

## Reconciliation

Both independently generated four candidate families and compatible labels:
broad model/metadata fragmentation REJECT; narrow operation separation DEFER;
same-file row Active-retirement helper ADOPT for review; rule clarification ADOPT.
A proposes a module for mutable DML plus immutable Red receipts. B proposes only
a same-file mutable DML function. These are materially different scopes/costs:
their shared DEFER does not mean agreement on a design. Preserve both variants.

Source/meaning/recovery context is largely available. Root inspected Business Design
entrypoints, scoped concepts, process/DFD, Decisions 0005/0006/0010/0013/0015,
boundary/queries/trusted binder, mutable/execution tests, deployed retirement
routine, CI definition and actual recovery/insert-only/retirement-test patches.
Not every document or production incident was examined; do not claim exhaustive
repository knowledge. One old Destination statement lists two models; current
Process/Decision 0006 and target-decision statements include insert-only. No new
meaning is inferred and no concept text is changed.

The unresolved narrow-seam question is comparative maintenance benefit, not
absence of business policy, source, history or PostgreSQL gates. The current mutable
block is contiguous; stable mapped-key checks precede reassessment elsewhere,
operation flags/results stay in the caller, and essential DML still needs DB tests.
A candidate-specific edit/reproduction sketch could demonstrate a useful reduction
or just another hop. It was not performed. Local exec unavailability is a separate
execution constraint, not the reason for DEFER: GitHub CI is available, and an
agent could do that comparison later. Stop here because the requested final
extension permits a precise supported DEFER, not because the experiment is
impossible or another candidate is automatically superior.

Do not equate shared ordering with a single large function. A common orchestrator
can retain order across helpers. Broad fragmentation's rejection does not reject
those narrow designs. Pattern knowledge generates alternatives; current contracts,
history and costs decide labels. See the existing engineering-basis source table;
no new external empirical result or pattern authority is claimed.

## Selected runtime candidate and preregistered comparison

Implement only the shared row-retirement helper in the same execution file.
Both current row branches contain the same release-Work-references query,
Active-delete query and exactly-one-row error. Parameters are Active ID and Link
ID; the existing bound-query closure is the only capability. Keep each row/routine
choice in its caller. Keep immutable Red Lineage after retirement and mutable
absence of Lineage. No model, source, callback, SQL, metadata routine, transaction,
count or public API changes.

Expected source-level benefit: row retirement protocol ownership falls from two
copies to one. Costs: one private function, three inputs, two calls and an
inspection hop; no new runtime file, export, dependency or framework. The row SQL
and optional PL/pgSQL routine remain deliberately separate evidence paths.

Before accepting completion:
1. Parse B2 and candidate TypeScript, compare the extracted protocol against both
   B2 blocks after only parameter renaming, then inline both candidate calls and
   remove the helper to compare all runtime tokens against B2.
2. Execute the actual helper with a scripted bound-query function. Check exact
   release/delete statement identity and parameter order, success for one row,
   error text for zero/multiple rows, and original exception identity/short-circuit
   behavior when release/delete fails. This proves local sequence only, not DB FK
   or rollback semantics.
3. Run existing PostgreSQL Verify and Issue 25 deployment gates; independently
   review changed behavior/claims. Never weaken existing tests.
4. Keep earlier comparisons pinned to their historical target revisions; the
   mapping-only checker does not prove this later extraction.
5. Stop once the bounded helper and procedure gates pass and the model candidate
   is explicitly recorded; no further candidate implementation this extension.

## Reopen evidence

Freeze candidates.json before the independent reopen probe. Use a clearly labeled
event replay with source-backed historical changes and synthetic incoming reports.
A historical event already included in B2 must not be advertised as new production
evidence. Ask whether each event addresses a missing question, merits reassessment
and warrants a label change. A relevant new task can merit reassessment without
closing its comparative gap. Do not implement a background watcher or auto-adoption.
