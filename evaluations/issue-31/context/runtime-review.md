# Independent Alder runtime review

## Exact task prompt

PR32 mk3008/velvet fresh independent Alder review required by AGENTS and user. Review baseline 4b895265d8262fafd7a95221a344a7de8c93841e -> candidate 69ee3d5d5f91cb59b67137b406c7ea947c78b4b1. GitHub connector tools via ALL_TOOLS, no exec. Read AGENTS, Business Design, relevant Decision Records then full docs/alder/review-knowledge.md and relevant implementation/DDL/tests. Runtime only private retireRowActive helper replacing identical row mutable-delete and immutable-Red release/delete/cardinality blocks. Also rule v3 and context candidate registry. Assess semantic safety, comparator adequacy and claim boundaries; no desired verdict. New workflow running, root handles CI. Don't edit branch. Return findings with paths and precise limits. Save exact prompt+review as GitHub blob (discover create_blob), send blob SHA for root to include evaluations/issue-31/context/runtime-review.md. Keep output bounded, retrieval acquired vs inspected candid.

## Follow-up scope message

Root inspected actual context CI logs (34850816952), found false green: tee masked assertion failure; mismatch only four trailing object commas when inlining. Fixed candidate helper formatting to preserve original trailing commas, workflow separate redirect and cat steps, commit d8cdd87fa1cc7633a89092e7d6db13251deb1635 now head. Please review this delta too. No assertion weakened. Prior run explicitly invalid evidence; document.

## Scope and inspection

Fresh agent context. Compared B2 4b895265d8262fafd7a95221a344a7de8c93841e with 69ee3d5d5f91cb59b67137b406c7ea947c78b4b1, then inspected the two-file delta through d8cdd87fa1cc7633a89092e7d6db13251deb1635. No branch edits or tests executed by this reviewer; GitHub source retrieval only. Root owns actual CI inspection.

Read AGENTS and Business Design entrypoint, Scope, concept index, Destination/Setting statement bodies, Dirty Key and Active Black meanings, intake DFD and Transfer Execution Process. Read Decisions 0003, 0005, 0012, 0013, 0015 and relevant current/history separation in 0014, then the complete pinned Alder review-knowledge v0.3 and Raw SQL Rules v0.3. Full fetched concept JSON includes relationship metadata beyond the scoped statements inspected. Batch output initially truncated; relevant concept statements and DFD/Process were reread explicitly. Decision 0014's wide table matrix was acquired but not exhaustively inspected. Incorrect initial guessed Decision/DDL filenames returned 404; actual paths were found and retrieved.

Inspected the full candidate execution boundary in chunks, both baseline retirement blocks, relevant query definitions and deployed retirement routine, full canonical db/ddl/work_item.sql and active_black.sql, mutable lifecycle assertions (historical references and preserved evaluated key) and fault-test bodies around retirement/processing/finalization/commit. The rest of the mutable test file and SQL files were acquired, not exhaustively reviewed. Inspected full retirement comparator, selection, v3 rule and candidate registry. Did not independently inspect all repository tests, historical commits named by registry, fresh-a/b responses, reopen replay, or CI logs. This is a scoped runtime/claim review, not a replication of the candidate tournament.

## Findings and sufficiency

No blocking semantic defect found in the reviewed runtime delta. This is conditional on required candidate CI gates, not a declaration they passed.

1. Q1/P1: A normal mutable disappearance still validates the physical-delete receipt before retirement; an immutable correction still validates Red, retires the old Active, records Red Lineage, then performs Black work. Both row calls remain in their original branches (boundary.ts:415,453 at reviewed candidate). The helper only awaits release, awaits delete and requires one returned row. Failure propagates to the unchanged coordinator rollback/durable-failure path. It does not finalize the Run or claim partial success. Current Work and historical Work references are cleared by the existing Active+Link predicate, while evaluated_destination_key_json remains untouched. Recovery ownership remains in executeTransfer.

2. Q2/P2: The reason for releasing references is an actual live FK, not a historical-data deletion policy. work_item.sql's single/composite Active FKs and nullable pointer, alongside active_black.sql's identity constraints, support release-before-delete. The unchanged queries.ts:63-73 applies the same Active+Link identity to both statements, without restricting release to the current Run. Thus historical evidence and current state remain separate as Decisions 0003/0005/0014 require. No new business authorization is needed for this private extraction.

3. Q3: The helper does not certify arbitrary stored DML; callers retain receipt cardinality/key checks and trusted SQL author's semantic responsibility. Immutable Lineage remains after retirement; mutable retirement still creates no Lineage. Routine/set behavior is not unified by this helper. Source-level ownership falls from two row protocol copies to one, not from all execution modes to one. Added cost is one private function, three inputs and two inspection hops; no new module/export/framework. No measured speed, production reliability, general maintainability or DB-performance gain follows.

4. Comparator adequacy: retirement-compare.mjs inspects the actual helper and exact calls, reconstructs the entire baseline token sequence, and separately asserts unchanged row SQL/routine source. This is a strong bounded transformation check when it actually passes. The ten query-fake cases exercise two labels with different IDs, each covering success, zero/multiple deletion rows and original release/delete exception identity/short circuit. Those labels are not executions of the two real callers; caller placement is supported by reconstruction and source inspection, while PostgreSQL tests remain necessary for FK/rollback/locking. Equality to baseline also preserves any baseline defects. No need to invent unrelated tests or an architecture requirement for this narrow seam.

5. Evidence correction reviewed: the initial 69ee3d5 comparator cannot pass its strict token comparison because helper object trailing commas differ from baseline, even though that punctuation is semantically harmless. Root reports run 34850816952 appeared green because tee masked failure; this reviewer did not read those logs and does not independently attest their contents. At d8cdd87 the helper retains the original object punctuation and the workflow runs Node with direct redirection in its own step, then cats the result. This propagates Node failure under the runner's normal error handling and leaves the comparator assertions unchanged. The earlier run must not be reported as successful comparator evidence. A fresh successful run/output remains required.

6. Claim/documentation follow-up: candidates.json still records row-retirement-helper as implementationStatus=planned and verificationStatus=not_run_on_candidate and rule as planned, despite implementation existing at reviewed candidate. These are conservative, not false success claims, but should be updated with final implemented revision and actual gate state, keeping disposition separate. Decision 0015's extension paragraph still calls the linked live rule v2 although it now contains v3; update that version reference or point explicitly to the frozen v2. The v3 rule itself correctly distinguishes technical missing comparison from business uncertainty, separates ADOPT from readiness, preserves narrow variants and avoids a mandatory full audit.

## Disposition

S: sufficiency confirmation for this extraction, with no new Business Design issue identified. Accept the source-level rationale for review; final completion requires actual passing PostgreSQL Verify, the preregistered Issue 25 gate, and corrected context comparator evidence tied to the final runtime revision. The two narrow model-operation variants remain comparative questions; this helper review does not reject or validate them. No merge or production acceptance is implied.
