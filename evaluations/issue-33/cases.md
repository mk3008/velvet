# Frozen context cases

Apply trial v4 independently. These cases contain facts/assumptions, not target
answers. For each: disposition; execution now/wait/trial; implementation state;
evidence effort and why; decisive evidence; missing observation / reopen trigger.
Read actual sources where facts could change the answer. Do not read peer outputs
or a root selection. Different execution choices need not imply different value.

## Shared repository evidence

Baseline `9f55499`. Read Decision 0015, issue-31/rule.md and context/candidates.json.
History anchors: `fbfe73e` mutable introduction, `6ffab6f` lost-COMMIT repair,
`8bb20a2` insert-only simplification. All available locally. The current B2 mutable
block selects UPDATE/DELETE, binds the locator, executes and validates cardinality
and the unchanged returned key. Caller retains pre-reassessment identity, Work,
retirement/Lineage, Processing and Run. Existing mutable.integration.test.ts
contains invalid return/locator and rollback tests. These are historical tests,
not evidence of a production incident. Prior comparison studies and PostgreSQL
results are in issue-31/context and multiaxis reports.

## C1: local repeated protocol, ordinary relevant work

Reconstruct the pre-retirement-helper candidate from issue-31/context evidence:
two identical release/delete/exactly-one protocols; three explicit inputs, no
new module/export, identical inline tokens and ten protocol traces; PostgreSQL
regressions passed for that extraction. Counterfactual task: maintain row metadata
in an ordinary feature task, helper relevant to edited code, no urgent release
constraint. Judge the candidate before deployment, not the already merged fact.

## C2: current B2 semantic grouping, dedicated structural investigation

B2 exact private same-file helper: five existing inputs (client, link, row,
mapped, active), no return state, no new export/file; extracts only mutable DML
preparation/execution/receipt. Caller retains all shared sequencing. Compare keep
against the review-only fixture in fixtures/candidate.ts.txt. Existing tests and
rollback are available; trial not yet deployed. Current task explicitly requests
refactor evaluation. The hypothesis is a named DML/receipt unit makes the overview
and malformed-return location easier to inspect. No fixture reduction or agent
performance result is assumed. Choose proportionate next action and disposition.

## C3: supported module boundary during urgent unrelated repair

Replay the mapping-only module from Decision 0015's multiaxis extension with its
recorded 39 predicate cases, nine placement traces and one directly callable
value-only owner. Counterfactual timing: an urgent lost-COMMIT response repair of
the kind in `6ffab6f`; mapping is unrelated, and causal isolation of that repair
is required. Assume the mapping candidate's exact code/evidence remains current.
It has one new module/internal export/two arguments and leaves date/SQL checks
and pre-Run placement at caller. Judge value independently from this task's scope.

## C4: current A2 module comparison incomplete

A2 as registered: mutable DML plus immutable Red execution/receipt in an internal
module; caller keeps shared state and ordering. Existing tests specify meanings.
The exact module interface and one maintenance comparison have not been built.
Unlike C3 there is no established direct-test/navigation benefit for this exact
seam. Current task is a dedicated structural evaluation, no incident constraint.
Decide whether B2 evidence alone can settle A2; identify smallest remaining work.

## C5: same B2 candidate during a narrowly scoped ordinary bug fix

Use C2's exact candidate and evidence, but task is to correct one mutable returned
key diagnostic. Reviewer requires the causal repair to remain independently
reviewable; extraction is not needed to fix or verify it. No production incident
is claimed. Explain whether this changes value, evidence effort or execution.

## C6: supported mapping boundary in feature-complete stabilization

Same candidate/evidence as C3. Counterfactual task explicitly includes finishing
structural cleanup before release, gates are available and there is time for
review. No freeze and no external contract change. Assume source/evidence current.

## C7: one-use forwarding abstraction

Counterfactual candidate at current boundary: replace `completed.add(context)`
with `markCompleted(completed, context)` whose whole body is that one statement.
No new invariant, validation, reuse, capability reduction or vocabulary difference
is proposed. It adds a two-input private function and a hop. Dedicated cleanup
is authorized. Source comparison is available, no timing benchmark conducted.

## C8: supported mapping boundary in release freeze

Same candidate/evidence as C3 and C6; counterfactual task explicitly admits only
release-blocking corrections, mapping is not one, source/evidence still current.
State the next event that could change execution without repeating the study.

## C9: deceptively local recovery extraction

Counterfactual candidate is a same-file helper that owns catch/recovery and accepts
mutable lifecycle state plus callbacks. Its proposed contract does not specify
what happens when COMMIT succeeds but its response fails, nor whether original
errors survive rollback failures. Existing source/test/history are available.
It is cheap to revert source, and the task is explicit cleanup. No implementation
or comparison yet. Decide whether locality alone is enough and what to inspect.
