# Run n2

## Execution outline (outline phase)

Before the loop, executeRowTransfer selects pending work using pendingSql or boundedPendingSql (233–241), resolves and validates each pending logical identity and stores its key text (242–249), executes the stored source SQL when work exists (250–254), and indexes source rows by projected logical key, rejecting duplicates (255–260). It initializes inserted/skipped counters and the completed-context set (261–263).

For each work item (264–488), it finds the destination link and model, current source row, duplicate context and Active Black row (265–277); maps source values with missing-value checks (278–287); determines no-op status, including an early mutable identity check and stored reassessment/comparison when active and source rows exist (288–336). It builds skip, Work and result fields and records Work (337–379). For non-skipped work it invokes the mutable destination helper for active mutable rows and retires metadata after deletion (381–393); performs and validates Red transfer and retirement/lineage for active immutable rows (395–432); and inserts eligible current rows, validates the returned key against mapping, and records Active/lineage/result metadata (433–483). It counts skips, marks the context completed, and records processing if not already recorded (485–487). It returns inserted/skipped counts (489).

Uncertainty at this stage: executeMutableDestination and retireRowActive have not been inspected, so their names and call sites establish their place in the flow but not their internal validations or SQL. The query symbols indicate intended bookkeeping roles, but their SQL implementation has not been inspected.

## Commands used so far

- `cat evaluations/issue-33/navigation-prompt.md`
- `node evaluations/issue-33/inspect.mjs n2 outline list`
- `node evaluations/issue-33/inspect.mjs n2 outline read executeRowTransfer`

## Locator answer

The shared mutable UPDATE/DELETE returned-key check is in `executeMutableDestination` at lines 517–523: it projects `result.rows[0]` onto `link.destination_key_columns` and deep-compares that key with `active.destination_key_json`, throwing on mismatch. The helper chooses UPDATE when `row` exists and DELETE otherwise (499–502). It validates stored statement presence, reserved parameter collision, original Active key binding, and UPDATE mapping consumption (503–513), executes SQL (514), and requires exactly one returned row and affected row (515–516) before checking the returned key.

Its ordering in `executeRowTransfer` is:

1. For active/current non-insert-only items, reassessment SQL and comparison finish first (309–335). Unchanged items become skips and never reach the mutable execution helper.
2. Work is recorded through `queries.workSql` at 374–379 for the non-skipped mutation. The routine skipped-metadata alternative only applies to skipped items.
3. Under `!skip` and `active && mutable`, the awaited helper runs (381–383), including execution, cardinality checking and returned-key checking.
4. Only after that helper succeeds does a mutable DELETE (`!row`) retire Active metadata (384–393). Routine mode uses `queries.retireMetadataSql` with `withLineage: false` (386–389). Otherwise `retireRowActive` releases Active references (532–535), deletes the Active row (536–539), and requires exactly one removal (540). Mutable UPDATE does not enter this retirement branch.
5. Completion marking and any remaining processing record occur afterward (486–487). A thrown returned-key error prevents those subsequent steps and deletion retirement in this function; transaction rollback behavior cannot be inferred from the inspected functions.

The separate, earlier mutable identity check must remain earlier: at 289–298, with both active and current source rows, it compares `projection(mapped, link.destination_key_columns)` with `active.destination_key_json`. It runs before reassessment and before no-op decisions based on comparison exclusions (299–335), and before Work recording. Thus changed mapped identity cannot escape checking because key columns were excluded from comparison or reassessment would label the item unchanged. This is a mapped-input identity check, distinct from the post-SQL returned-row identity check. There is also logical-source identity validation before reading current source values (242–249); it does not replace either mutable destination check.

## Remaining uncertainties

The inspected code establishes control flow and validation ordering. SQL bodies for query symbols and the internals of binding/projection were not inspected; exact database effects and transaction/rollback scope are therefore not asserted. No edits are proposed.

## Additional commands used

- `node evaluations/issue-33/inspect.mjs n2 locator read executeMutableDestination`
- `node evaluations/issue-33/inspect.mjs n2 locator read retireRowActive`
- Shell `mkdir -p` and heredoc writes created this response, then appended the locator answer; they did not inspect source.
