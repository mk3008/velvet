# Run n1

## Outline (recorded before locator inspection)

`executeRowTransfer` (208–514) first loads bounded or unbounded pending work (233–241), resolves and validates each logical key before source execution (242–249), executes the stored source SQL only when work exists (250–254), builds a current-row map rejecting duplicate keys (255–260), and initializes counters and a completed-context set (261–263).

For each item (264 onward), it locates the link/model and source row, detects duplicate link/key contexts, fetches Active metadata unless duplicate, and maps source columns (265–287). It establishes no-op status; for active/current non-insert-only rows it checks mutable mapped identity, validates reassessment inputs, runs stored reassessment and comparison, and derives no-op status (288–336). It classifies skip/result/work fields and records Work or routine skipped metadata (337–380). For non-skips it executes the applicable mutable update/delete, immutable red transfer, and/or black insertion paths, validating returned row counts and identities and issuing relevant retirement/metadata queries (381–508). It counts skips, marks the context completed, records processing unless already recorded, and ultimately returns inserted/skipped counts (509–513).

Uncertainty at this outline stage: SQL query bodies, `bindStoredSql`, and the internals of helpers have not been inspected; query/helper names alone do not establish their effects or transaction behavior. The outline describes visible calls and branches.

## Locator answer

The mutable UPDATE/DELETE returned-key check is inside `executeRowTransfer`, in `if (!skip)` / `if (active && mutable)` (381–382). The branch selects `generated_update_transfer_sql_body` or `generated_delete_transfer_sql_body` according to source-row presence (383–386), checks/binds the Active key and required mapped update arguments (387–397), then executes at 398. It checks both returned-row array length and `rowCount` equal one (399–400). At 401–407 it compares `projection(result.rows[0], link.destination_key_columns)` to `active.destination_key_json` using `isDeepStrictEqual`, throwing an operation-specific mismatch error. `projection` (62–70) also rejects absent, undefined, or null key columns; it constructs only the requested key-column object.

This returned-key check is **after reassessment and Work recording**. For an UPDATE with active and current rows, reassessment executes at 319, comparison at 326–332, and the no-op decision is assigned at 335. A no-op skips mutation entirely. DELETE has no current row, so the reassessment block at 289 does not run. Both actual mutable operations record Work through `queries.workSql` at 379 before entering the mutation branch; the alternative routine skipped-metadata call is only for skips.

The check is **before retirement**. Only the mutable DELETE path enters 408–417: routine mode calls `queries.retireMetadataSql` with `withLineage: false` (410–413), otherwise it calls `retireRowActive` (415). That helper visibly calls `queries.releaseActiveReferencesSql` first (522–525), then `queries.activeDeleteSql` (526–529), and requires exactly one removed row (530). The mutable UPDATE path does not retire Active here. A returned-key/count failure throws before these retirement calls and before completed-context/processing recording at 510–511.

The separate, earlier mutable identity check at 289–298 must stay before reassessment/no-op classification: it compares the **mapped current destination key**, not the mutation return value, to `active.destination_key_json`. It enforces stable mutable identity even when destination key columns are excluded from comparison (290, 299, 329). Deferring that validation until after reassessment would allow a no-op decision to skip the mutation and thus skip the returned-key guard entirely. It also precedes Work recording. Separately, pending logical-key validation at 242–249 occurs before source current values are queried at 251–253.

## Remaining uncertainties

I inspected only the assigned reader output. The SQL bodies behind query symbols, SQL binding implementation, and caller transaction handling were not inspected. Consequently I can establish call/validation order and that exceptions prevent later calls in this function, but cannot claim whether an already executed mutation or Work write is rolled back. The routine retirement query's internal behavior is not established from its name; its invocation and parameters are visible. No edits are proposed.

## Commands used

All commands ran from `/workspace/scratch/1b5c72679d22/velvet`.

1. `cat evaluations/issue-33/navigation-prompt.md`
2. `node evaluations/issue-33/inspect.mjs n1 outline list`
3. `node evaluations/issue-33/inspect.mjs n1 outline read executeRowTransfer`
4. Wrote the outline above with a shell heredoc to `evaluations/issue-33/runs/n1.md`, before any locator reads.
5. `node evaluations/issue-33/inspect.mjs n1 locator read retireRowActive`
6. `node evaluations/issue-33/inspect.mjs n1 locator read projection`
7. `node evaluations/issue-33/inspect.mjs n1 locator read executeRowTransfer`
8. Appended the locator answer, remaining uncertainties, and command record to `evaluations/issue-33/runs/n1.md` with a shell heredoc.
