# Issue 47 — independent evidence and scope review

No blocking findings. The reviewed synthesis and original evaluator report support **VELVET-SPECIFIC** as a bounded application of existing guidance. No substantive correction is required, and no product change, larger benchmark or new review rule follows.

## Scope and reviewed inputs

This is a fresh-context, known-result evidence/scope review, configured `gpt-6-astra / low`, of product/design revision `300ba133377cacf2bfd0a475436c3746cc0d3fbf`. It is not a second blind evaluation or an agent-performance trial. I read AGENTS, the Business Design entrypoint, Scope, intake DFD, Transfer Execution Process, Dirty Key/Transfer Setting concepts, Decisions 0016/0017 and copied Alder review knowledge before inspecting implementation. Decision 0013 supplied the narrower set-phase contract.

Reviewed `protocol.md`, `metadata.json`, `evaluator-prompt.txt`, `runs/evaluator.md`, `runs/navigation.jsonl` and `README.md`. The README, evaluator report and navigation hashes match `metadata.json`'s `reviewer.reviewedInputs`; the navigation JSONL parses. The tracked product/design diff against the target is empty: the tracked additions at review time are the Issue 47 protocol and prompt. Other evaluation files are untracked pending parent publication. This review writes only this file and does not commit or push.

The navigation record is evaluator-authored and explicitly disclosed as such, not an independently authenticated complete tool transcript. Its corrections preserve the failed brace expansion, truncated reads and incorrectly named historical helper. The metadata also discloses post-dispatch protocol commit timing and the parent's evidence-precision follow-up. These limits prevent treating this as an entirely unassisted or preregistered blind experiment.

## Consequential claim checks

- **C1, mutable locality and recovery:** `src/features/execute-transfer/boundary.ts` places early mapped-identity rejection before comparison, awaits `executeMutableDestination` after Work creation and before retirement, and keeps Run finalization/recovery in `executeTransfer`. Actual diff `67fc622` extracts the same mutable operation without moving those responsibilities. The pre-split catch/finalization in `10549ce^` retains the same outer recovery ownership. `queries.ts` guards failure marking with `run_status = 'running'`. The authored COMMIT-response-loss fixture in `tests/features/execute-transfer/mutable.integration.test.ts` executes the server query before throwing and expects persisted success. The report correctly treats this as inspected test intent, not an executed observation or unconditional rollback guarantee. Retaining B2 and leaving A2/overview dispositions unchanged is fair.
- **C2, executor composition:** `_shared/query-executor.ts` declares an optional transaction callback; `create-transfer-setting/workflow.ts` checks only that it is callable and runs resolution/parent/link writes through its callback. `adapters/pg/sql-client.ts::fromPg` only binds and queries. `tests/adapters/pg/features.integration.test.ts` supplies a single `pg.Client` and savepoints inside an outer transaction; the registration unit fake merely invokes `operation(this)`. Neither establishes deployed composition. The report correctly separates these from `TransferExecutionClient`, whose transaction commands are owned by `executeTransfer`. Historical diffs `11b8ce9` and `db54426` support the change from external preparation to binding while preserving caller-owned transaction policy; the report expressly declines semantic-equivalence claims about uninspected historical helper internals. No DI/interface defect is established.
- **C3, captured configuration versus execution:** `boundary.ts` inserts `phase.evidence`, commits Run creation, reacquires configuration and compares it before work. `set-phase/config.ts::loadSetPhase` builds `{ engine, setting, links }`; `queries.ts::runSql` omits that configuration, as does the historical `ff0cbbd^` Run INSERT. `db/ddl/run.sql` permits legacy null configuration. `set-phase/execute.ts` uses the lower invocation/profile admission cap and can return on empty admission. The report therefore correctly distinguishes selected master configuration, actual phase execution, and replay/root-cause evidence. `run_arguments` receives the argument object, not the separate `maxDirtyKeys` option. Resolver, source/destination/Active state, deployed functions and environment are not recovered merely by reading the captured masters. Decision 0013's immutable/text-key restriction also prevents an invalid claim that row users can simply switch engines for diagnostics. The set-phase COMMIT-loss fixture supports the synthesis's recovery example but was not run here.
- **C4, removed forwarding seams:** Actual diff `eed0092` removes exactly the two named execution forwarders, each of which only called `executor.query` with its named QuerySource. The paired receipt boundaries retain awaiting, cardinality and parsing, with `satisfies QueryParams` at direct dispatch. The claimed removed policy-check target is supported by source, without claiming measured time savings or fewer semantic obligations. Decision 0017's result remains unchanged; SQL/validation boundaries are not implicated.

## Sufficiency and completion conditions

| Condition | Result |
| --- | --- |
| Contrasting incident walkthroughs | Met: four explicitly authored packets contrast receipt/recovery locality, concrete executor composition, historical configuration and removed forwarding seams. No real incident or root cause is invented. |
| Comparison with existing judgments | Met: Decisions 0016/0017 and actual historical diffs are compared. Existing incident-diagnosis guidance is acknowledged, not replaced with a code-size straw man. |
| Material change or explicit negative result | Met: C3 makes the next historical-evidence question and stopping condition concrete; C1/C4 explicitly report no new structural benefit and C2 corrects the initial ambiguous executor route. This is a local application result, not proof that the prompt caused improved discovery. |
| Limits and overapplication | Met: absent runtime evidence, unmeasured time/performance/frequency, selected scenarios and visible prior judgments are disclosed. The report rejects callback-shape atomicity, capture-as-execution, hash-as-approval and current-master-as-history false positives. It does not demand universal tracing, DI removal, snapshots or recurring audits. |
| One allowed disposition | Met: the original report ends with `VELVET-SPECIFIC`; the synthesis agrees and does not propose Alder wording. |

Under Alder Q1–Q3/P1–P2/S, this is a sufficiency finding: the evaluation preserves continuation/recovery and handoff guarantees, distinguishes authored scenarios from observations, returns implementation choices to existing meaning, and stops unsupported additional requirements. No identified alternative business interpretation changes an output or owner responsibility here. This does not approve new business meaning or establish completeness across all possible incidents.

## Remaining administrative work and limits

The synthesis links `verification.md`; its final checks, reviewer hash, publication and PR/CI results are parent follow-up, not substantive evidence reviewed here. This report neither anticipates a passing CI result nor substitutes for the existing repository verification gate. I ran no tests, PostgreSQL operations or benchmarks and inspected no production incident data. Later administrative additions should preserve the reviewed evaluator artifacts and keep test execution separate from this source/history review.
